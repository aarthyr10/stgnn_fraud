# Install CPU-only torch wheels (keeps the Streamlit Cloud build under its
# disk limit; harmless on macOS/Windows, which resolve CPU wheels anyway).
--extra-index-url https://download.pytorch.org/whl/cpu

# Streamlit and runtime
streamlit>=1.31
plotly>=5.18
pandas>=2.1
pyarrow>=14
numpy>=1.26
python-dotenv>=1.0

# Modelling
torch>=2.1
torch-geometric>=2.4
scikit-learn>=1.3

# Development
pytest>=7.4
